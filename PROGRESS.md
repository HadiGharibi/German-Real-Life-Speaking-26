# German Real-Life Speaking — Course Progress

## Current Lesson

- current_lesson: `DE-ALLTAG-003`
- status: `active`
- activated_on: `2026-08-24`

## Rules

- این فایل وضعیت درس جاری دوره را مشخص می‌کند.
- تا زمانی که `status: active` است، درخواست «درس امروز» نباید باعث تولید دوبارهٔ همان درس شود.
- در این حالت باید فایل متناظر با `current_lesson` مستقیماً از GitHub خوانده و همان نسخهٔ Canonical نمایش داده شود.
- تولید درس جدید فقط زمانی انجام می‌شود که درس جاری برای روز بعد/مرحلهٔ بعد بسته شده و `current_lesson` به شمارهٔ جدید منتقل شود.
- هنگام ایجاد درس جدید، ابتدا درس طبق `LESSON-STANDARD.md` ساخته و در GitHub ذخیره و verify می‌شود؛ سپس `current_lesson` به همان شناسه تغییر می‌کند.
- حافظه و سابقهٔ چت مرجع تعیین محتوای درس جاری نیستند؛ GitHub Source of Truth است.
