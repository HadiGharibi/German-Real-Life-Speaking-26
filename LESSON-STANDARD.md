# German Real-Life Speaking — Canonical Lesson Standard

این فایل مرجع اجباری ساخت و بازیابی همهٔ درس‌های `DE-ALLTAG` است.

## Source of Truth

- GitHub تنها مرجع اصلی و Canonical محتوای دوره است.
- هر شناسهٔ `DE-ALLTAG-XXX` فقط یک درس دارد و هرگز برای محتوای دیگری دوباره استفاده نمی‌شود.
- محتوای حافظه، سابقهٔ چت یا نسخه‌ای که قبلاً در پاسخ ChatGPT دیده شده، جایگزین فایل Canonical در GitHub نیست.
- قبل از ساخت درس جدید، Repository، درس‌های موجود و `Spaced-Repetition-Marker`های قبلی باید واقعاً خوانده شوند.
- اگر یک درس موجود اصلاح شود، همان شماره و همان فایل به‌روزرسانی می‌شود؛ نسخهٔ موازی با همان شماره ساخته نمی‌شود.

## هدف آموزشی

درس‌ها برای مکالمهٔ طبیعی و واقعی روزمره طراحی می‌شوند، نه زبان کتابی. هدف، تبدیل Satzmusterها و Kollokationenهای پرتکرار به زبان فعال و خودکار است.

هر درس باید یک موقعیت واقعی و نسبتاً محدود داشته باشد. «کامل بودن» به معنی جمع‌کردن چند موضوع مستقل در یک درس نیست. موضوعات مستقل باید به درس‌های جداگانه منتقل شوند.

## ساختار Canonical هر درس

هر درس جدید باید از ابتدا، تا حدی که برای موضوع طبیعی و مفید است، این ستون فقرات را داشته باشد:

1. `Situation / موقعیت واقعی` — سناریوی دقیق و هدف عملی درس.
2. `Kernmuster` — الگوهای اصلی که باید خودکار شوند.
3. `Natürliche Alltagssätze` — جمله‌های طبیعی و پرکاربرد مرتبط با همان موقعیت.
4. `Wichtige Wörter & Kollokationen` — افعال، عبارت‌ها و ترکیب‌های مهم.
5. `Sprachgefühl` — تفاوت زبان طبیعی با ترجمهٔ تحت‌اللفظی یا فرم رسمی؛ واژه‌های گفتاری مهم.
6. `Mini-Dialoge` — چند گفت‌وگوی کوتاه برای تثبیت الگوها.
7. `Natürlicher Dialog` — یک مکالمهٔ واقعی و نسبتاً کامل، ترجیحاً در موقعیت خانوادگی/روزمرهٔ مرتبط.
8. `Spaced Repetition / Aktivierung` — بازیابی طبیعی ساختارهای درس‌های قبلی در سناریوی جدید.
9. `Active Recall` — تولید آلمانی بدون نگاه‌کردن به پاسخ.
10. `Transformation Drill` — تغییر سریع الگوها برای افزایش سرعت تولید جمله.
11. `Geführtes Role-play` — نقش‌آفرینی هدایت‌شده با اطلاعات مشخص.
12. `Freies Role-play` — مکالمهٔ آزاد بدون متن آماده.
13. `Aufgabe für den Alltag` — استفادهٔ واقعی همان روز.
14. `Kernmuster — خروجی فعال درس` — فهرست کوتاه ساختارهایی که باید بعد از درس فعال باشند.
15. `Spaced-Repetition-Marker` — مشخص کند چه ساختارهایی باید در درس‌های آینده دوباره ظاهر شوند.
16. `Abschluss-Checkpoint` — توانایی عملی مورد انتظار پس از پایان درس.
17. `عبارت کلیدی درس` — یک جمله/تبادل بسیار طبیعی که هستهٔ درس را نمایندگی کند.

شمارهٔ بخش‌ها می‌تواند بر اساس نیاز موضوع تغییر کند، اما اجزای آموزشی بالا نباید بدون دلیل حذف شوند.

## قواعد کیفیت

- آلمانی باید طبیعی، امروزی و مناسب گفت‌وگوی واقعی در آلمان باشد.
- از جمله‌های مصنوعی و بیش‌ازحد کتابی پرهیز شود.
- فارسی باید معنای طبیعی جمله را منتقل کند، نه الزاماً ترجمهٔ کلمه‌به‌کلمه.
- `Kernmuster`ها محدود، پرتکرار و قابل استفاده در موقعیت‌های دیگر باشند.
- درس نباید صرفاً فهرست لغت باشد؛ تمرکز بر chunks، Satzmuster و Kollokationen است.
- تفاوت‌های مهم گفتاری مانند `was/etwas`، `Nee/Nein`، `kurz`، `eigentlich`، `gerade` و Modalpartikeln در صورت ارتباط توضیح داده شوند.
- مثال‌ها باید تا حد ممکن با زندگی واقعی خانواده سازگار باشند.
- محتوای یک درس آینده نباید بی‌دلیل وارد درس فعلی و باعث تکرار شود.

## قانون Cumulative Learning

دوره مجموعه‌ای از درس‌های مستقل نیست؛ باید cumulative باشد.

در هر درس جدید:

- `Spaced-Repetition-Marker`های درس‌های قبلی بررسی شوند.
- تعدادی از ساختارهای قبلی به‌شکل طبیعی در موقعیت جدید دوباره استفاده شوند.
- تکرار نباید به شکل کپی‌کردن بخش‌های قدیمی باشد؛ ساختار قدیمی باید در context جدید فعال شود.
- ساختارهای مهم در فواصل مختلف دوباره برگردند تا به زبان فعال تبدیل شوند.

نمونهٔ زنجیره:

- `DE-ALLTAG-001`: `schon / noch / noch nicht`, `gerade`, `Soll ich …?`
- `DE-ALLTAG-002`: این الگوها هنگام آماده‌کردن کودک دوباره فعال می‌شوند.
- `DE-ALLTAG-003`: `noch` و ساختارهای قبلی در خرید و هماهنگی خانوادگی دوباره استفاده می‌شوند.

## Active Recall و Drill

پاسخ‌های Active Recall باید دقیقاً از زبان فعال درس ساخته شوند.

Transformation Drill باید کوتاه، سریع و pattern-based باشد؛ هدف آن تحلیل گرامر نیست.

مثال:

`Haben wir noch Milch?`

→ `Haben wir noch Brot?`
→ `Haben wir noch Eier?`
→ `Haben wir noch Windeln?`

## Role-play

هر درس در صورت مناسب‌بودن باید دو سطح داشته باشد:

- هدایت‌شده: نقش و اطلاعات مشخص.
- آزاد: فقط موقعیت و چند cue؛ بدون متن آماده.

هدف نهایی این است که زبان‌آموز بدون وابستگی به متن بتواند مکالمه را ادامه دهد.

## Alltag-Aufgabe

تکلیف باید قابل اجرا در همان روز و در زندگی واقعی باشد. هدف حفظ کل درس نیست؛ هدف استفادهٔ واقعی از چند Kernmuster منتخب است.

## «درس امروز» — Mandatory GitHub Save-First Workflow

این workflow اجباری است. هنگام درخواست `درس امروز` نباید ابتدا یک درس موقت در چت تولید شود.

ترتیب دقیق:

1. **READ:** ابتدا Repository واقعاً بررسی شود.
2. **READ STANDARD:** فایل `LESSON-STANDARD.md` واقعاً از GitHub خوانده شود؛ اتکا به حافظه یا سابقهٔ چت کافی نیست.
3. **READ LESSONS:** آخرین درس‌های Canonical موجود و `Spaced-Repetition-Marker`های لازم از GitHub خوانده شوند.
4. **RESOLVE NUMBER:** شمارهٔ بعدی معتبر مشخص شود. شمارهٔ موجود overwrite نمی‌شود مگر کاربر صراحتاً درخواست اصلاح همان درس را داده باشد.
5. **GENERATE ONCE:** فقط یک نسخهٔ نهایی مطابق Canonical Standard ساخته شود. نسخهٔ ساده، draft یا موازی به کاربر نمایش داده نشود.
6. **SAVE FIRST:** همان متن کامل ابتدا در مسیر `lessons/DE-ALLTAG-XXX.md` در GitHub با موفقیت ذخیره شود.
7. **VERIFY:** بعد از ذخیره، همان فایل از GitHub دوباره خوانده شود تا مشخص شود نسخهٔ Canonical واقعاً ثبت شده است.
8. **DISPLAY FROM GITHUB:** فقط محتوای فایل verify‌شدهٔ GitHub به کاربر نمایش داده شود. پاسخ نباید از draft، حافظه یا نسخهٔ قبل از ذخیره ساخته شود.
9. اگر ذخیره یا verify در GitHub شکست خورد، درس به‌عنوان Canonical نمایش داده نشود و صریحاً گفته شود ثبت GitHub موفق نشده است.
10. متن نمایش‌داده‌شده برای `درس امروز` و متنی که بعداً با `درس روز X` بازیابی می‌شود باید از یک فایل Canonical واحد در GitHub بیاید؛ بنابراین نباید دو نسخهٔ متفاوت داشته باشند.
11. پاسخ درس مستقیماً با عنوان `DE-ALLTAG-XXX — ...` شروع شود و توضیح فنی اضافه قبل یا بعد از درس نداشته باشد، مگر کاربر درخواست کند.

خلاصهٔ workflow:

`READ GitHub → READ STANDARD → READ PRIOR LESSONS/MARKERS → GENERATE ONCE → SAVE TO GITHUB → VERIFY FROM GITHUB → DISPLAY VERIFIED FILE`

## Direct Lesson Retrieval — درخواست «درس روز X»

این حالت تولید محتوا نیست.

اگر کاربر بگوید `درس روز 1`، `درس روز ۲`، `درس روز 3` یا هر شمارهٔ مشخص دیگری:

1. درخواست **بازیابی مستقیم درس موجود** است، نه درخواست تولید درس جدید.
2. GitHub باید در همان درخواست واقعاً خوانده شود؛ استفاده از حافظه، سابقهٔ چت یا بازسازی محتوا ممنوع است.
3. فایل متناظر از GitHub خوانده شود؛ برای مثال `درس روز 3` یعنی `lessons/DE-ALLTAG-003.md`.
4. محتوای Canonical همان فایل **کامل و بدون بازنویسی، خلاصه‌سازی، ارتقا، اصلاح یا تولید نسخهٔ جدید** نمایش داده شود.
5. پاسخ مستقیماً با عنوان `DE-ALLTAG-XXX — ...` شروع شود.
6. قبل یا بعد از متن درس هیچ مقدمه، توضیح دربارهٔ GitHub، گزارش وضعیت، پیشنهاد، جمع‌بندی یا سؤال اضافه نشود، مگر اینکه کاربر صراحتاً درخواست کند.
7. این قانون حتی در یک چت جدید و بدون تاریخچهٔ قبلی برقرار است: GitHub تنها Source of Truth محتوای درس است.
8. اگر فایل آن شماره در Repository وجود نداشت، درس جدید با آن شماره حدس زده یا ساخته نشود؛ فقط کوتاه گفته شود که آن درس هنوز در GitHub ثبت نشده است.

خلاصهٔ workflow:

`درس روز X → READ lessons/DE-ALLTAG-XXX.md FROM GITHUB → DISPLAY EXACT FILE`

## Canonical Baseline

`DE-ALLTAG-001`, `DE-ALLTAG-002` و `DE-ALLTAG-003` پس از استانداردسازی، baseline عملی این قالب هستند. درس‌های بعدی باید از نظر عمق آموزشی، تمرین فعال و پیوستگی دوره حداقل هم‌سطح آنها باشند.
